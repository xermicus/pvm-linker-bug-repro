[package]
name = "pvm-linker-bug-repro"
version = "0.1.0"
edition = "2021"

[dependencies]
polkavm-linker = { git = "https://github.com/koute/polkavm" }
revive-runner = { git = "https://github.com/xermicus/revive", branch = "cl/disable-validation", default-features = false }
