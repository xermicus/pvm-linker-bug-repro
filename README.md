This expects a LLVM build with rv32e target support in `$PATH` (like for example [the first two steps here](https://github.com/xermicus/revive?tab=readme-ov-file#installation)).

Then:

```bash
 RUST_LOG=runtime=debug cargo run
```
